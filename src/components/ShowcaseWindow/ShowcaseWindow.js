import React from 'react';
import classes from './ShowcaseWindow.module.scss';

const showcaseWindow = props => {
  return (
    <div className={classes.ShowcaseWindow}>
      Showcase Window
    </div>
  );
};

export default showcaseWindow;