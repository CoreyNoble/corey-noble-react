import React from 'react';
import classes from './CoutdownTimer.module.scss';

const coutdownTimer = props => {
  return (
    <div className={classes.CoutdownTimer}>
      Coutdown Timer
    </div>
  );
};

export default coutdownTimer;