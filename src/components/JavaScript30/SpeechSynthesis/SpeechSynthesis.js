import React from 'react';
import classes from './SpeechSynthesis.module.scss';

const speechSynthesis = props => {
  return (
    <div className={classes.SpeechSynthesis}>
      Speech Synthesis
    </div>
  );
};

export default speechSynthesis;