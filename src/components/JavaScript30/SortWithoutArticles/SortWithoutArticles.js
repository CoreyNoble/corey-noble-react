import React from 'react';
import classes from './SortWithoutArticles.scss';

const sortWithoutArticles = props => {
  return (
    <div className={classes.SortWithoutArticles}>
      Sort Without Articles
    </div>
  );
};

export default sortWithoutArticles;