import React from 'react';
import classes from './SlideInOnScroll.scss';

const slideInOnScroll = props => {
  return (
    <div className={classes.SlideInOnScroll}>
      Slide In On Scroll
    </div>
  );
};

export default slideInOnScroll;