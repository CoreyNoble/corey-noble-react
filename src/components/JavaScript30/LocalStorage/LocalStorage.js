import React from 'react';
import classes from './LocalStorage.scss';

const localStorage = props => {
  return (
    <div className={classes.LocalStorage}>
      Local Storage
    </div>
  );
};

export default localStorage;