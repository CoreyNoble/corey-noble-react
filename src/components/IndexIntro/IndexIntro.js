import React from 'react';
import classes from './IndexIntro.scss';

const indexIntro = props => {
  return (
    <div className={classes.IndexIntro}>
      Index Intro
    </div>
  );
};

export default indexIntro;