import React from 'react';
import classes from './AboutCategory.scss';

const aboutCategory = props => {
  return (
    <div className={classes.AboutCategory}>
      About Category
    </div>
  );
};

export default aboutCategory;