import React from 'react';
import classes from './ScrollableSection.scss';

const scrollableSection = props => {
  return (
    <div className={classes.ScrollableSection}>
      Scrollable Section
    </div>
  );
};

export default scrollableSection;